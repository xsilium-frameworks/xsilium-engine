CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

# add sources
SET(SRCS
  ${SRCS}
    ./binreloc.c
)

ADD_LIBRARY(binreloc STATIC ${SRCS})

INSTALL(TARGETS binreloc LIBRARY ARCHIVE DESTINATION lib)