project(NVIDIA_NVAPI)

set(NVAPI_INCLUDE_DIR "${NVIDIA_NVAPI_SOURCE_DIR}/include" CACHE PATH "")
if (XSILIUM_PLATFORM_X64)
  set(NVAPI_LIBRARIES "${NVIDIA_NVAPI_SOURCE_DIR}/lib64/nvapi64.lib" CACHE PATH "")
else ()
  set(NVAPI_LIBRARIES "${NVIDIA_NVAPI_SOURCE_DIR}/lib/nvapi.lib" CACHE PATH "")
endif ()
mark_as_advanced(NVAPI_INCLUDE_DIR NVAPI_LIBRARIES)
