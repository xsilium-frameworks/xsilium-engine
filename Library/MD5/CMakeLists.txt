CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

# add sources
SET(SRCS
  ${SRCS}
    ./md5.cpp
)

ADD_LIBRARY(md5 STATIC ${SRCS})

INSTALL(TARGETS md5 LIBRARY ARCHIVE DESTINATION lib)